# Service interno del frontend (puerto 80). Lo usa el Ingress.
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: hosting
spec:
  selector:
    app: farutech-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP